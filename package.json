{
  "name": "airtable-cypress-take-home-assignment",
  "version": "1.0.0",
  "description": "Rama's take home cypress assignment for airtable",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "lint": "npx eslint cypress/integration cypress/support",
    "lint --fix": "npx eslint --fix cypress/integration cypress/support" 
  },
  "dependencies": {
    "cypress": "^7.3.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/cypress": "^8.0.2",
    "eslint-plugin-cypress": "^2.12.1"
  }
}
